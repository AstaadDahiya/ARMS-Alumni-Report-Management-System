import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function DonationsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Donations</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the Donations page.</p>
      </CardContent>
    </Card>
  );
}
